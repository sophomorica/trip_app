
<p><%= @location.image %></p>
<p><%= @location.fees %></p>
<div>




<div class="ui card">
  <div class="content">
    <div class="center aligned header">
<h1><%= @location.name %></h1>
</div>
    <div class="center aligned description">
    <%= @location.address ? @location.address.street : "" %>
    <%= @location.address ? @location.address.city : "" %>
    <%= @location.address ? @location.address.state : "" %><br/><br/>
      <p>Lat<% @location.address ? @location.address.lat : "" %></p><p>long
      <% @location.address ? @location.address.long : "" %></p>
    </div>
  </div>
  <div class="extra content">
    <div class="center aligned author">
      <img class="ui avatar image" src="https://semantic-ui.com/images/avatar/small/jenny.jpg"> <%=@trip.name%>
    </div>
  </div>
</div>







<div class= "button_div">

<%= link_to "Edit Location", edit_trip_location_path([@trip, @location]), class: "ui button" %>
<%= link_to "Delete Location", trip_location_path(@trip, @location), class: "ui button", method: :delete %>
<%= link_to "Back to Locations", trip_locations_path(@trip), class: "ui button" %>
<%= link_to "Add Address", new_location_address_path(@location), class: "ui button" %>
<%= link_to "Edit Address", edit_location_address_path(@location,@location.address.id), class: "ui button" %>
<%= link_to "Back to All Locations", trip_locations_path(@trip), class: "ui button"%>

</div>